export enum RemoveItem {
  oneDay,
  oneWeek,
  twoWeeks,
  oneMonth,
  twoMonth,
  halfYear,
  oneYear,
  Manual,
}

export enum LanguageEnum {
  system,
  en,
  zh,
}
export enum ShortcutAction {
  ADD,
  DELETE,
}
